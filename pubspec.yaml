name: adwaith
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase dependencies
  firebase_core: ^2.30.0
  firebase_database: ^10.4.8
  cloud_firestore: ^4.17.3 # Added Firestore

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  intl: ^0.18.0
flutter:
  uses-material-design: true
